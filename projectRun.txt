# Option 1: Docker (Recommended)
cd ~/MyFolder/Project/Messenger
docker compose up -d

# Option 2: Local Development (Without Docker)
# Terminal 1 - Backend:
cd ~/MyFolder/Project/Messenger/backend
source venv/bin/activate
uvicorn app.main:app --reload --port 8000

# Terminal 2 - Frontend:
cd ~/MyFolder/Project/Messenger/frontend
npm run dev
